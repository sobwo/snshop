<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myteam.myapp.persistance.ShopService_Mapper">
	<sql id="searchCategory">
		<choose>
			<when test="value==0">
				 
			</when>
			<when test="value==1">
				AND category in 
				<foreach collection="filter" item="item" separator="," open="(" close=")">
					#{item}
				</foreach>
				OR categoryName in 
				<foreach collection="filter" item="item" separator="," open="(" close=")">
					#{item}
				</foreach>
				
			</when>
		</choose>
	</sql>	
	
	<sql id="align">
		<choose>
			<when test="index==0">
				order by a.goodsNo desc 
			</when>
			<when test="index==1">
				order by a.price desc
			</when>
			<when test="index==2">
				order by a.price asc
			</when>
			<when test="index==3">
				order by a.interestNum desc
			</when>
			<when test="index==4">
				order by a.goodsNo desc 
			</when>
		</choose>
	</sql>
	
	
	<sql id="setInterest">
		<choose>
			<when test="value==1">
				set interestNum = interestNum + 1
			</when>
			<when test="value==2">
				set interestNum = interestNum - 1
			</when>
		</choose>
	</sql>
	
	<select id="goodsSelectAll" resultType="gv">
		select * from goods a inner join productImg b on a.goodsNo = b.goodsNo
		where b.goodsNo IN (select goodsNo from productImg group by goodsNo having imgNo=min(imgNo)) order by a.goodsNo desc
	</select>
	
	<select id="goodsSelectOne" parameterType="int" resultType="gv">
		select * from goods where goodsNo = #{goodsNo}
	</select>

	<select id="imgSelectOne" parameterType="int" resultType="piv">
		select * from productImg where goodsNo = #{goodsNo} order by imgNo asc
	</select>
	
	<select id="filterList" parameterType="java.util.List" resultType="piv">
		select * from goods a inner join productImg b on a.goodsNo = b.goodsNo
		where b.goodsNo IN (select goodsNo from productImg group by goodsNo having imgNo=min(imgNo))
 
		<include refid="searchCategory" />
	</select>
	
	<select id="alignList" parameterType="java.util.List" resultType="piv">
		select * from goods a inner join productImg b on a.goodsNo = b.goodsNo
		where b.goodsNo IN (select goodsNo from productImg group by goodsNo having imgNo=min(imgNo))
		<include refid="searchCategory" />
		<include refid="align" />
	</select>
	
	<select id="recommentList" parameterType="HashMap" resultType="piv">
		
		select * from goods a inner join productImg b on a.goodsNo = b.goodsNo
		where b.goodsNo IN (select goodsNo from productImg group by goodsNo having imgNo=min(imgNo))
		and categoryname = #{0} and a.goodsNo not in(#{1}) order by a.interestNum desc limit 10
	</select>

	<!-- interest -->
	<select id="interestCnt" parameterType="Integer" resultType="Integer">
		select COUNT(*) from interest where memberNo = #{0} and goodsNo=#{1}
	</select>
	
	<insert id="insertInterest" parameterType="Integer">
		insert into interest(memberNo,goodsNo) values(#{0},#{1});
	</insert>
	
	<delete id="deleteInterest" parameterType="Integer">
		delete from interest where memberNo={0} and goodsNo=#{1}
	</delete>
	
	<update id="updateInterest" parameterType="Integer">
		update goods
		<include refid="setInterest"></include>
	</update>
	
	<select id="selectInterestAll" parameterType="Integer" resultType="iv">
	select a.goodsNo, a.goodsName, a.price, b.size from goods a inner join interest b on a.goodsNo = b.goodsNo where b.memberNo = #{0}
	</select>

</mapper>