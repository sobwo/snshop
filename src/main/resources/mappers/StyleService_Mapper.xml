<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myteam.myapp.persistance.StyleService_Mapper">


<select id="boardSelectAll" parameterType="int" resultType="bv">
	<!-- select * from Board innerjoin where memberNo not in {memberNo} nad memberNo and memberNo=  -->
<!-- select * from board (조인 sql) where memberNo not in {로그인시 memberNo(나)} and memberNo = (select followingMemberNo from following where memberNo = {})		 -->
</select>


</mapper>